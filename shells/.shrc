# source
for rc in "$HOME/.shrc.d/0dirs.sh" "$HOME/.shrc.d/aliases.sh" "$HOME/.shrc.d/dev.sh"; do
    [ -f "$rc" ] && . "$rc"
done

# Prompt
PS1="\u@\h:\w \\$ "

# bat theme
export BAT_THEME="GitHub"

# fzf
# eval "$(fzf --bash)"
