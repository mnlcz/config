Vim
===

This section documents Vim's configuration.

Overview
--------

The configuration is a single file:

``editor/vimrc``

Which expects to be symlinked to ``~/.vim/vimrc``.

The idea is to have a small and simple configuration, serving as a
simpler version of the Neovim setup, intented to be used for simple 
tasks.