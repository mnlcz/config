#!/bin/sh
unit=$(cat /dev/sndstat | grep -i "G435" | grep -o "pcm[0-9]*" | grep -o "[0-9]*")
if [ -n "$unit" ]; then
    sysctl hw.snd.default_unit=$unit
    mixer -f /dev/mixer${unit} vol.volume=1.0:1.0
    mixer -f /dev/mixer${unit} pcm.volume=1.0:1.0
fi
